<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>itunes</title>
  <link rel="stylesheet" href="">
</head>
<body>

<% for (let i = 0; i <music.length; i++) { %>
    <h4><%= music[i].artistName %></h4>
    <img src="<%= music[i].artworkUrl60 %>"/>
<% }; %>
<!--   </div>
      <div class="firstData"> Results:
    <% music.forEach(function(x){ %>

    <p>Artist: <%= x.artistName %> </p>

    <form> search bar
    <insert type="text" name="music" placeholder="music">
    <input type="submit" value="search">
    </form>

    </div>
  <% }) %> -->
  </div>
</body>
</html>
